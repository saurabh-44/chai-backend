/* The constant holds the name of your MongoDB database, which is "videotube". This value will be appended to your MongoDB connection string when connecting to the 
database. */
export const DB_NAME = "videotube"
